import SidebarLayout from "@/Layouts/SidebarLayout";

export default function Dashboard({ ...data }) {
    console.log(data);

    return (
        <SidebarLayout>
            <main>Hi</main>
        </SidebarLayout>
    );
}
